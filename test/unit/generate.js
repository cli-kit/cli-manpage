var path = require('path');
var expect = require('chai').expect;
var pkg = path.normalize(path.join(__dirname, '..', '..', 'package.json'));

describe('cli-manpage:', function() {
  it('should create man pages', function(done) {
    done();
  });
})
